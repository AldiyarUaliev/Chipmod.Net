@inject NavigationManager Navigation

@code {

    [Parameter]
    public string Path { get; set; }

    [Parameter]
    public bool ForceLoad { get; set; }

    protected override void OnInitialized()
    {
        Navigation.NavigateTo(string.IsNullOrWhiteSpace(Path) ? "/" : Path, ForceLoad);
    }

}